if( !window.Show ) throw new Error( 'Requires assets/javascript/show.js to be defined. And to create an array called window.Show' );

(function( $, undefined ){

	var s = new Slideshow( Show );

})( Zepto )

